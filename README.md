# DockerUbuntuGTestGMock
Little example on using Docker to compile and execute GTest/GMock tests.

After downloading this repo, go to the directory where it is located and execute:

```
$ cd ChatMediaServer
$ ./compileAndExecuteTestsInDocker.sh -t ChatMediaServerTest -c vrcordoba/ubuntu-gtest-gmock
```
